[package]
name = "pubsub-client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"]}
log = "0.4"
quinn = { workspace = true }
rustls = { workspace = true }

[dev-dependencies]
rcgen = "0.13"
bytes = "1"
