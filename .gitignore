target
.idea

# Android FFI build outputs (cross-compiled .so libs + generated uniffi
# bindings); regenerated by build-ffi.sh, never committed.
jniLibs/
out/

.aider*

deploy/deploy_key

# Unused legacy relay TLS material — the dumb relay holds no keys (see
# relay/src/lib.rs). Kept out of the repo to avoid shipping a private key.
certs/
